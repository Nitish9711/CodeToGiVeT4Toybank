{
    "name": "toybank_backend",
    "version": "1.0.0",
    "main": "",
    "scripts": {
        "start": "node index.js",
        "install-server": "cd backend && npm install && cd .. ",
        "heroku-postbuild": "cd backend && npm install && npm run build"
    }
}